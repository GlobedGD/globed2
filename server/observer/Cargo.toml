[package]
name = "observer"
version = "0.1.0"
edition = "2021"

[dependencies]
globed-shared = { path = "../shared" }
globed-derive = { path = "../derive" }
observer-shared = { path = "../observer-shared" }
esp = { path = "../esp" }
egui = "0.29.1"
